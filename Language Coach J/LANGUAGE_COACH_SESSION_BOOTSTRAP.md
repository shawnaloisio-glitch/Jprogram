# Language Coach Session Bootstrap

Companion to `CLAUDE.md`. `CLAUDE.md` holds standing rules (role, permission
mode); this file holds current project state. Expected to go stale between
sessions and gets refreshed at each wrap-up.

---

## Current phase

"1000 mile" outline done (2026-08-07), and real known-word data now exists
under `bootstrap/` — the known-level bootstrap from design spec §8 is
actually underway, not just planned. Built this session:
- `bootstrap/known_kanji_first400_speedrun.txt` — 400 kanji Owner has
  confirmed as known, from `C:\Japanese Learning\Anki projects\Kanji
  Speedrun 1200.txt` (Order 1–400).
- `Shawn/teppei_1-50_self_assessment.tsv` — 1,374 lemmas from an
  existing con-teppei vocab-frequency dataset, frequency-sorted. Owner
  assessed the first 352 rows: 81 confidently known out of context, 271
  recognized-but-unconfident (not a negative signal — see
  `LANGUAGE_COACH_OWNER_PROFILE.md`). **Data-quality caveat: the source
  vocab baseline was generated by NotebookLM, not Jprogram's parser,
  unverified — see `WORKING_LIST.md`.**
- **`bootstrap/lingq_known_words.jsonl` — 6,633 words from Owner's actual
  LingQ account**, status decoded to a 1-5 known-level scale (5,365 / 304
  / 142 / 773 / 49). By far the richest known-word data pulled so far.
  Extraction required a browser-console script (native export and direct
  API pagination both hit real dead ends) — full reusable procedure in
  `LANGUAGE_COACH_LINGQ_EXPORT_PROCEDURE.md`, needed again periodically
  until QuadRead's own grading stage exists.
- **`LANGUAGE_COACH_OWNER_PROFILE.md` (new file)** — Owner's language
  journey, current exposure by source, and the modality-specific level
  breakdown (visual=hard carry, audio=strong carry, reading-alone=weakest/
  real floor). Directly shapes how known-word data should be interpreted.
- **`LANGUAGE_COACH_CONSUMPTION_LOG.md` (new file)** — source-level
  consumption index with independent Watched/Listened/Read flags per
  entry, owned by Language Coach (not a Jprogram Source Registry change).

**Also built this session, after the above:**
- **User contract question resolved** — no upfront formal design; document
  frequent routines after the fact instead (design spec §5). Not something
  left to build.
- **`tools/analysis/` — isolated copies of Jprogram's 7 analyzer modules**
  (frequency/distribution/exposure/expression/chunk/sentence_metrics/
  comparison), plus `corpus_loader.py`/`output_writer.py`. Not a live
  import — Language Coach owns these copies outright, zero runtime
  dependency on Jprogram's code (only ever reads Jprogram's *data*).
  Adapted to group by **surface form**, not lemma, per §8a. Smoke-tested
  against synthetic records — all 9 modules run clean. Ready to point at
  real corpus data the moment Jprogram outputs it (expected before the end
  of the session Owner was in when this was built, "unless something
  disastrous happens (again)").
- LingQ confirmed (not just recommended) as the primary known-vocabulary
  source, level ≥4 = known. Grammar known-state tracking explicitly
  deferred to a future point.
- A full Jprogram per-source metadata schema review (5 gaps resolved) —
  see design spec §12.

**Further built/decided this session, after the real-data testing above:**
- **Leech handling (design spec §9a)** — a second, distinct decision
  branch in the value criteria: words with exposure well past the
  threshold that are *still* unacquired stop being targeted via reading
  and get flagged for handoff to Anki instead. Not built yet, needs the
  bounded-session tracking first.
- **`LANGUAGE_COACH_OWNER_PROFILE.md` substantially expanded** — Owner's
  full goals now on record: input-only (no speaking/writing, ever),
  staged target anime→TV→reading but reading expected to land first in
  practice (real structural reason: the whole pipeline is a text-analysis
  pipeline, no equivalent audio-analysis tooling exists or is planned),
  Krashen-informed theoretical grounding, reading-as-domain-specific-SRS
  framing, a real daily-pace number (1,500 LingQ words/day at full
  capacity — a strong candidate to anchor the "bounded session" exposure
  unit, see `LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md`), and why this
  project is currently prioritized (enjoyment, real subscription-cost
  savings, a deliberate short-term-slower-for-long-term-faster trade).
  Read that file before assuming anything about scope or calibration.

Still true: Language Coach has no standalone application — the chat
session itself is the interface.

## Last decisions and why

- **QuadRead ("MiniLingQ") confirmed to replace QuadRead as the pipeline's
  reader stage (2026-08-08).** QuadRead (`C:\AI Development Projects\
  QuadRead`) is a real, working browser-based reader built the same day —
  not a concept, Phases 1–3.5 done. It already defines the downstream
  import contract concretely: a **Grammar to learn** list (plain text, one
  term per line, longest surface form — its own TODO names Language Coach
  as the source it's waiting on) and a **Known words** import that reads
  Language Coach's actual `lingq_known_words.jsonl` `term` field directly
  (level <4 skipped). This resolves most of the old "output format to
  QuadRead" open question concretely. Cross-project docs (`Shared\
  ECOSYSTEM_OVERVIEW.md`, QuadRead's own folder) still name QuadRead and haven't
  been updated yet — outside Language Coach's write boundary, needs
  Owner's go-ahead for those specific locations. See `WORKING_LIST.md`.
- **Working folder created at `C:\AI Development Projects\Language Coach`**
  (2026-08-06), mirroring Jprogram's and QuadRead's documentation pattern, so
  this project can be picked up in its own chat with continuity. Confirmed
  2026-08-06: this folder (not QuadRead's) is Advisor's home for Language Coach
  work going forward — Owner is pointing the session's working directory
  here.
- **Development model confirmed (2026-08-06): no Coder/OC stage for this
  project — Owner + Advisor only.** Once a task moves past outlining/
  planning and Owner approves it, Advisor implements directly (Advisor and
  Coder are the same actor here), unlike Jprogram's strict Advisor/Coder
  separation. See `CLAUDE.md` Role section.
- **Runtime API convention confirmed (2026-08-06):** if the built program
  makes AI API calls, development uses Anthropic's API; production (after
  development is complete) switches those calls to DeepSeek's API. Doesn't
  settle whether Language Coach needs AI calls at all — still open, see
  design spec §4.
- **"1000 mile" outline completed (2026-08-07)**, replacing the v0
  fragments doc with `LANGUAGE_COACH_DESIGN_SPEC.md` v1. Headline
  decisions: Language Coach has no standalone UI (the chat session is the
  UI, §5); the real deliverable is a bootstrap + user contract + a set of
  deterministic Python tools Advisor calls directly; two analysis modes
  (single-piece value report, cross-corpus i+1 matching, §7); value
  criteria are illustrative and expected to be tuned over time, not fixed
  (§9); Python confirmed as the tools' language, matching Jprogram (§11).
- **Read-access boundary broadened (2026-08-07):** effectively
  unrestricted — any location on disk, plus the open web — not just
  `C:\AI Development Projects\`. Write access unchanged: this workspace
  only, explicit permission required for anywhere else. Prompted by
  discovering real, useful data sources outside the original boundary
  (`Content Collection/` for nijapanese/nihongo-jikan catalogs, and an
  OneDrive folder with existing con-teppei transcripts/vocab data). See
  `CLAUDE.md`.
- **Known-level bootstrap actually started (2026-08-07)**, per design spec
  §8 — see Current phase above for the two artifacts built.

## Open risks / unresolved questions

All in `WORKING_LIST.md`. Headline: **real data exists at scale, the
pipeline is proven end to end, and the first real value reports already
surfaced real gaps** — the test corpus is now **321 parsed sources**
(`Jprogram Workspace/jsonl/`, `clean_text_nhgjm-idNNNNN`, NJ catalog ids
0-325; 43,627 sentences, 267,461 words), and the earlier 8-source
simulation runs (session value calc, per-source known-coverage ranking)
validated the bounded-session concept and the difficulty-ranking signal
against real data. Full detail on the findings lives in
`LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md` and `WORKING_LIST.md`, not
here — this section stays a pointer instead of re-accumulating content
every session.

Loose ends, not blockers: **5 ids failed parsing** (56, 235, 262, 297,
306 — real NJ titles known, see WORKING_LIST.md); expressions are empty
corpus-wide (decided limitation — no good deterministic Japanese
expression library, cost-effective LLMs were flaky); and
`LANGUAGE_COACH_SUGGESTIONS_FOR_JPROGRAM.md` items 1-3 are queued for
Owner to add to Jprogram's own worklist, not yet actioned. Naming map
for user-friendly cross-reference: `Jprogram Workspace/jsonl/rename_log.csv`
(`NHGJM idNNNNN` ↔ real NJ title).

## Next immediate task

Two real filtering gaps (grammatical fragments, loanword handling) need
solving before a value report can be trusted without manual review — see
`LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md` for full detail on both. Once
those exist, or are consciously deferred: derive a real (not illustrative)
exposure/value threshold using the bounded-session framing (real
candidate unit: 1,500 LingQ words/day, see Owner Profile), per design
spec §8 step 4, §9, and §9a (leech handling). The 321-source corpus now
supports real Mode 2 (cross-corpus) work — the earlier "needs a real
multi-episode set" blocker is resolved; remaining scaling work is the
full-catalog import and the progression map.

## Session closed 2026-08-08

Wrapped per Owner's explicit request (context window full). All
language-related findings from this session committed to files above;
the local-AI/GPU/ROCm exploration tangent at the end was deliberately
excluded — general curiosity, not project-relevant, not recorded.

