# Working List

A running queue of small, concrete items to check, decide, or act on —
distinct from `LANGUAGE_COACH_SESSION_BOOTSTRAP.md`, which holds major
architecture/state/scope.

**Format:** one item per entry. Mark `[x]` when resolved and note where the
resolution landed.

---

## Open

- [x] **Real deterministic-parser data now exists and works end to end** (2026-08-07/08). `clean_text_teppeibeginner_ep004.jsonl` — 160 sentences, real `ginza-ja_ginza-5.2.0` output, not the old DeepSeek stale data. Ran `tools/analysis/frequency_analyzer`, `distribution_analyzer`, `exposure_analyzer`, `sentence_metrics`, `chunk_analyzer` against it for real: 301 distinct surface forms, 1,184 total word occurrences, 374 distinct chunks, correct spacing stats (e.g. の: 72 gaps, mean 16.0 words apart) and first-seen locations. First genuine proof the whole pipeline works on real content, not synthetic or stale data. Episodes 2-3 failed processing (per Owner's Processing tab); episode 1 (old, DeepSeek) and episode 4 (new, ginza) both exist — only episode 4 should be treated as current/trustworthy.
- [x] **Test corpus built from the full NJ intake batch** (2026-08-08). `Jprogram Workspace/jsonl/` now holds **321 parsed corpus files** (`clean_text_nhgjm-id00000`–`id00325`, NJ catalog ids), 43,627 sentences / 267,461 words / 143,168 chunks, 0 expressions (see the expression-limitation item). **5 ids failed parsing: 56, 235, 262, 297, 306** (real NJ titles known: 娘とおしゃべり, カタカナ禁止ゲーム初級#1, 二つの真実と一つの嘘, 日本語でゲーム Neko Odyssey EP01/EP04) — they have Source Registry entries and rename-log rows but no jsonl. **File size = episode length** (3 min → 20+ min; top file ~3,146 words, avg 833) — size is a content-length signal, not noise. **Naming map for user-friendly cross-reference: `jsonl/rename_log.csv`** (326 rows: `NHGJM idNNNNN` ↔ real NJ title, e.g. id00000 = "102 - Japan and Brazil 日本とブラジル.html"; covers the 5 failures too). ⚠️ One real title shows mojibake (`日本語でゲーム` for id00306, should be 日本語でゲーム) — watch when title-matching. Generated pipeline folders were cleared 2026-08-08 (responses/Analysis/requests/jobs/Logs/results) per Owner — corpus + Sources/ + Source Registry/ + `indexes/jprogram.db` + `Config/` retained; regenerable from the corpus.
- [x] **First real Mode 1 value report produced** (2026-08-08) — cross-referenced episode 4 against LingQ known-word data (level≥4): 224/301 surface forms not known. Top candidate: 抹茶 (matcha), 21 occurrences, evenly spaced (mean 2.75 sentences apart), thematically central to the episode. See `LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md` for the real findings this surfaced (bounded-session exposure, grammatical-fragment noise, loanword handling).
- [x] **Filter short grammatical fragments from value-criteria candidates — shape-noise class DONE 2026-08-09** — real finding 2026-08-08: た/お/でし-type tokenizer fragments show up as high-occurrence "unknown" words alongside genuine vocabulary. **Built:** `tools/analysis/candidate_filter.py` — deterministic shape rules (digits １/２, single-kana お/あ/た/つ, fillers うーん/えー, punctuation, explicit tokenizer-split fragments でし). **Validated:** 37/37 checks (findings-doc cases + boundaries), real-data effect: Beginner unknowns 11,897 → 11,590 (307 shape-noise dropped). **Design note:** dictionary-gating investigated and rejected (JMdict contains the fragments AND omits conjugations — cannot separate noise from vocab). Wire-up: applied on top of SQLite query results in the report path.
- [ ] **Loanword filtering — DEFERRED by Owner decision 2026-08-09** (real finding 2026-08-08: カフェ/アクセサリー counted as equivalent-value to genuine new vocabulary). **Owner decision:** katakana/loanwords are NOT filtered — they are legitimately "unknown until encountered"; Owner grades or ignores them in the QuadRead reader as he reads (the 3-case false-friend distinction is a reader-grading concern, not a report-filter concern). `candidate_filter.py` deliberately keeps all katakana (include_katakana=True default; a False switch is stubbed for future reuse). Revisit only if reader-grading proves insufficient. See `LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md`.
- [x] **Filtering solution (consolidated) — resolved 2026-08-09.** Fragment class: DONE (`candidate_filter.py`, validated). Loanword class: deferred by Owner (reader-grading approach). Remaining: integration into the report flow (currently applied on DB query results; the report path should use it as the default filter). The "digits/katakana edge cases at corpus scale" finding is covered by the shape rules (digits) + Owner decision (katakana kept). See `LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md`.
- [ ] **Grammar discovery solution — deterministic preferred** (Owner 2026-08-08). The "top 20 grammar patterns to highlight" query proved chunks can't produce grammar patterns (GiNZA chunks are surface fragments; only 9/33 probed constructions survive as exact chunks). Viable deterministic approach: regex pattern-matching over corpus word sequences (te-form+しまう, verb+ながら, sentence-end でしょう, たことがある...) with real occurrence counts per pattern, ranked across sources. Distinct from expression extraction (the unsolved problem) — this is rule-based discovery over the word-level data we already have. Explore whether it can also feed grammar known-state tracking later. See the grammar-pattern deferral note in the Resolved section for the investigation history.
- [ ] **Exposure needs to be computed over a bounded "next session's worth of content" set, not single-piece** — confirmed empirically 2026-08-08 (episode 4 alone: no word reached anywhere near the illustrative ≥30, max was 21), then refined by Owner: not an abstract whole-series scan, specifically the batch of content Owner is actually about to consume (e.g. "next hour"). Ties Mode 1 to a real session and gives Mode 2 a concrete job (assembling that candidate set). Still explicitly theoretical/unvalidated — see `LANGUAGE_COACH_VALUE_CRITERIA_FINDINGS.md`. Blocked on more episodes existing in Jprogram to test against (only ep004 currently trustworthy/current).
- [ ] **Build leech handling — a second decision branch in the value criteria** (confirmed 2026-08-08, design spec §9a). Not a threshold tweak: words with exposure well past the threshold (illustrative: 60) that are *still* unacquired should stop being targeted through reading entirely and get flagged for handoff to a different method (Anki cloze/sentence cards). Needs cumulative/bounded-set exposure tracking first (the item above) before this can be evaluated.
- [x] **Deadline: nijapanese.com access ends ~2026-09-25** (subscription). Confirmed 2026-08-07 this is Language Coach's own tracking to keep, not a Jprogram input. **Update 2026-08-08:** the "0/672 transcripts extracted" figure in the original entry is **stale** — `Content Collection/LIBRARY_STATUS.md` (updated 2026-08-07) plus direct inspection of `D:\Natural Japanese media\` shows extraction is essentially complete: 904/906 text pieces across all four levels (Complete Beginner 147/147, Beginner 339/341 — 2 filename collisions, Intermediate 310/310, Advanced 108/108, excl. Yuki). The 09-25 cutoff still matters for re-fetching/reconciliation against the live site, but the content itself is already local.
- [x] **Nihongo Jikan ruby-HTML intake cleaner (Jprogram-side)** — **DONE 2026-08-08** (Owner confirmed; last 8 corpus sources are its output — NJ ids 10/19/21/81/111/114/421/461). Original spec (for reference): strip `<ruby>`/`<rt>` to plain text, **no furigana kept** (he reads without furigana deliberately). See `LANGUAGE_COACH_SUGGESTIONS_FOR_JPROGRAM.md` item 4. Remaining note: 22 NJ videos (ids 882, 888, 891-914) have no transcript on the site at all — recheck periodically. NJ content can now flow through Jprogram normally.
- [ ] **Natural Japanese audio surplus = Yuki's migrated content (cross-reference done 2026-08-08)** — `D:\Natural Japanese media\Audio\` has more files than subtitles (intermediate 553 audio vs 310 subs; complete-beginner 240 vs 147). Cross-referenced against the NJ catalog by title: **210/243 unmatched intermediate audio files match NJ video titles (Yuki's content, moved NIJ→NJ, largely unrenamed; 33 unmatched — possibly renamed/removed)**. Complete-beginner matches ~61 of 182; the rest includes ~17 other-teacher files (Chieko/Momo/Seika/Meika — NIJ content with genuinely missing local subtitles) and ~100+ no-match files (likely old CIJ-archive/rename leftovers). Implication: don't double-count NIJ audio as distinct from NJ content; reconciliation is housekeeping on `D:\` (see `_ready_to_delete\`, Owner-side).
- [ ] **Rebuild the self-assessment approach at surface-form granularity** — `Shawn/teppei_1-50_self_assessment.tsv` was built from NotebookLM's lemma-grouped baseline, now the wrong granularity per the surface-form decision (design spec §8a). Not redone yet.
- [ ] **Language Coach → QuadRead highlight-list handoff (the actual output to build)** — confirmed 2026-08-09: QuadRead is waiting on Language Coach's file. Two deliverables per `Shared/ECOSYSTEM_OVERVIEW.md` + QuadRead `todo/TODO.md`: (1) **grammar-term list** — plain text, one term per line, longest surface form, imported via "Grammar to learn" (but note: grammar highlighting was deferred by Owner 2026-08-08 — revisit whether this means the grammar list is also deferred, or only the in-report grammar analysis); (2) **known-word highlight list** — the value-report output that drives the reader's highlighting. QuadRead can already import `lingq_known_words.jsonl` (reads `term`, skips level <4). **Blocker confirmed:** this handoff is gated on the filtering work (fragments + loanwords) so the highlight list is trustworthy. The QuadRead→Language Coach feedback (dictionary export) is the *later* direction — it becomes meaningful once the reader has accumulated real known-state from use.
- [ ] **Grammar known-state tracking — deferred to a future point** (confirmed 2026-08-07), after real corpus/learning-history data exists in the system. Method not decided: may replicate the vocabulary approach (LingQ export, self-assessment) or a different method once there's actual data to work from. LingQ's Vocabulary page has a separate "Phrases" tab, unexplored — worth checking first when this comes up.
- [ ] Re-run the LingQ export periodically as Owner keeps studying (status data goes stale) — procedure documented in `LANGUAGE_COACH_LINGQ_EXPORT_PROCEDURE.md`, needed until QuadRead's own grading stage exists.
- [ ] `extended_status` field shows `0` vs. `None` inconsistently across status levels with no clear pattern found yet — doesn't affect the confirmed level-mapping, but flagged as an unresolved loose end (see procedure doc).
- [ ] **Pull exposure stats (`count`/`episode_count`) for the 81 confidently-known words vs. the 271 recognized-but-unconfident ones** in `Shawn/teppei_1-50_self_assessment.tsv` — still pending. Note this data is lemma-grouped (NotebookLM baseline), so revisit alongside the surface-form rebuild above rather than in isolation.
- [ ] Whether to extend the self-assessment past row 352, and/or add a third tier (confident-known / recognize-probably-fine-in-context / true-unknown) instead of binary y/blank, now that the recognize-tier distinction is understood
- [ ] **Reconcile `teppei_1-50_vocab_baseline_v2.jsonl` against a real Jprogram-parsed dataset once one exists** (2026-08-07) — it's NotebookLM output, not Jprogram's parser; Owner can't currently verify its accuracy. One spot-check found real discrepancies: claimed count for 皆 was 239, raw substring count in the transcript was 127; the source transcript file (`Clean AI transcript 1-50.txt`) only actually contains episodes 1–28, not 1–50 as the filename and `episode_count` values imply. Treated as "good enough for now" per Owner (2026-08-07) — compare against real parsed output once con-teppei goes through Jprogram's actual pipeline.
- [ ] Whether the §8 bootstrap is a one-time thing or a recurring recalibration as Owner's level grows
- [ ] Whether/how to extend the §8 bootstrap to nijapanese.com (difficulty ≤45 confirmed as the boundary, but "beginner" tier transcripts are 0/672 extracted per `Content Collection/LIBRARY_STATUS.md`) and LingQ (first 40 mini stories — no local data, needs manual import, Owner's account) — **note (2026-08-07): nijapanese's difficulty scale is calibrated to listening/video, likely the wrong ruler for reading-specific i+1 — see `LANGUAGE_COACH_OWNER_PROFILE.md`. Also note the difficulty-45 structural cliff (speech rate + scaffolding loss + grammar-tier jump all at once), not a smooth continuum.**
- [ ] Whether/how Language Coach should track modality separately (listening vs. reading vs. audio-assisted-reading), not just a single known-word list — raised by the profile work in `LANGUAGE_COACH_OWNER_PROFILE.md` (2026-08-07); design spec §4/§8 currently assume one known-word list, undifferentiated by modality
- [ ] **Per-item consumption logging tool** — Owner floated this 2026-08-07 ("maybe even make a logging tool"), not yet designed. Would upgrade `LANGUAGE_COACH_CONSUMPTION_LOG.md` from hand-maintained source-level aggregates to real per-item/per-episode entries with dates.
- [ ] **Con-teppei "first 50" vs. the 28 episodes actually in `Clean AI transcript 1-50.txt`** — Owner states 50 episodes consumed; the transcript file we have only covers 1–28. Unresolved whether 29-50 exist elsewhere or actual consumption was fewer episodes. See `LANGUAGE_COACH_CONSUMPTION_LOG.md`.
- [ ] What "domain" means in "word distribution across the corpus and domains" — source type, genre, difficulty tier, or something else (§12)
- [ ] Whether Jprogram's flat+SQL DB migration has actually landed yet, or Language Coach's tools are blocked waiting on it (§12) — note: the §8 bootstrap may not need this, since it can run against already-consumed material directly
- [ ] Detailed design of the bootstrap file and tools themselves (§5, §12) — the outline only establishes that these are the deliverable. (User contract dropped from this item — resolved separately below, "document as routines emerge" instead of upfront design.)
- [ ] Exact SQL schema / connection mechanism for Language Coach to reach Jprogram's database (§12)
- [ ] Whether grammar points use the same value criteria as words, or their own rule (§9)
- [ ] Whether exposure/spacing for value criteria is computed within a single content piece only, or cumulatively across a series (§9)
- [ ] Which of §9's researched candidate value signals actually get used, beyond the core exposure/spacing/known-list formula (§9, §12)
- [ ] Whether Language Coach needs any AI (LLM) calls at all, vs. purely deterministic tools — current direction leans deterministic (§11)
- [ ] **QuadRead's actual import contract now known (2026-08-08) — reconcile Mode 1/§9 report shape against it.** QuadRead (`C:\AI Development Projects\QuadRead`, "MiniLingQ") is a real, working reader app — confirmed by Owner 2026-08-08 to **replace QuadRead** in the pipeline (QuadRead docs/ecosystem overview now stale, not yet updated — see below). Its two import paths, already built:
  - **Grammar to learn**: plain text, one term per line, longest surface form — its own TODO lists this as waiting specifically on a Language Coach file.
  - **Known words**: reads `lingq_known_words.jsonl`'s `term` field directly, skips level <4 — i.e. it already expects Language Coach's actual LingQ export file, no reformatting.
  This answers most of the old "output format to QuadRead" open question (§10) concretely rather than abstractly. Still open: the *value-report* → grammar-term-list step itself isn't built (depends on the filtering-gap/threshold work above).
- [x] **Cross-project docs updated after the QuadRead/QuadRead decision** — Owner confirmed 2026-08-08: QuadRead retired, QuadRead (originally an unrelated coding-platform test, built for ~$0.20) adopted as the real reader stage. `Shared\ECOSYSTEM_OVERVIEW.md` updated to reflect QuadRead throughout (pipeline diagram, handoff contracts, new "Retired" section for QuadRead). QuadRead's own folder also updated — `RETIRED.md` added, banners on `CLAUDE.md`/bootstrap.
- [x] **QuadRead coverage review (2026-08-08)** — checked QuadRead's actual import code (`js/app.js`), not just its docs: only two term-list imports exist, `known` and `grammar` (no word-level "highlight list"/"words to learn" import). Two gaps identified, both resolved by Owner same session:
  - **Word-level highlight-list import — resolved by decision, not by building anything.** Owner is fine grading organically in QuadRead (read → mark chunks New/Learning/Known as encountered) rather than Language Coach pre-injecting a ranked candidate list into the reader UI. This simplifies Language Coach's own output contract — Mode 1's value report is something Owner reads and acts on directly, not a file format QuadRead needs to understand.
  - **Feedback export path (QuadRead's per-device known-state → Language Coach) — confirmed a real, non-optional gap, not "minor/interim-mitigated" as first assessed.** Reasoning: once Owner grades live in QuadRead, QuadRead — not LingQ — becomes the actual known-state ground truth going forward; the LingQ export only covers the *bootstrap* moment. No export path or reusable procedure exists yet (unlike `LANGUAGE_COACH_LINGQ_EXPORT_PROCEDURE.md` for LingQ). **Not yet built — real next-build item**, priority TBD alongside the filtering-gap/threshold work above.
- [ ] **Seed "100% known" list for QuadRead's Known-words import — source decided (2026-08-08): LingQ top tier only (level 5, not the ≥4 threshold used for i+1 analysis).** Reasoning: LingQ's own tokenization has "oddities" against Jprogram's more consistent chunking; a narrower, higher-confidence cut minimizes bad matches landing in QuadRead's dictionary (which would incorrectly hide real unknown words from Owner). **Note the resulting fork:** design spec §8b's "level ≥4 = known" threshold stays as-is for Language Coach's own i+1 comparison logic; this level-5-only cut is a *separate, narrower* threshold specific to the QuadRead seed-list export. Not yet built — needs a small extraction step off the existing `bootstrap/lingq_known_words.jsonl` (filter to level 5, no new LingQ pull required).
- [x] **QuadRead → Language Coach feedback format decided (2026-08-08): reuse QuadRead's existing Backup export as-is, no new QuadRead code needed.** `exportBackup()` (`QuadRead/js/app.js:902` (STALE as of 2026-08-10, re-verify)) already writes `minilingq-backup-YYYY-MM-DD.json` with flat `dictionary[]` (words) and `grammar[]` arrays — each entry `{surface, language, status, note, addedAt, updatedAt}`. Word ladder: `new/learning/known/ignored`; grammar ladder: `new/learning/known` (entries reset to `unknown` are deleted from the store, so the export only ever contains touched words — nothing to filter out on that front). Language Coach reads these two arrays directly; `defs[]` (cached dictionary lookups) is irrelevant to us.
- [ ] **Graduated/fuzzy exposure threshold by QuadRead status tier — new design direction (2026-08-08), folds into the existing "bounded session exposure" and "leech handling" items above.** Owner's framing: don't collapse QuadRead's status into a binary known/unknown cut for i+1 — use it to *discount the exposure threshold needed per word* instead. Illustrative numbers from Owner (explicitly not final): a word at `new` might need ~30 exposures before being considered adequately reinforced, one already at `learning` only ~20 more; open to adding a finer-grained stage if 3 tiers proves too coarse. This is a real formula change to design spec §9's value criteria, not just a data-format decision — needs to be designed properly alongside the exposure-threshold derivation work already queued above, not bolted on ad hoc.
- [ ] **Reusable QuadRead export procedure doc, mirroring `LANGUAGE_COACH_LINGQ_EXPORT_PROCEDURE.md`** — not written yet. Would cover: click Backup in QuadRead → where the file lands → how Language Coach parses `dictionary[]`/`grammar[]`. Structurally simpler than the LingQ procedure (flat array, no status-code decoding), so this is a short doc once it's actually needed — per the standing "document routines after the fact, not upfront" pattern, hold off until Owner is actually grading in QuadRead and a real export exists to test against.

## Resolved

- [x] Full "1000 mile" outline pass — done 2026-08-07, see `LANGUAGE_COACH_DESIGN_SPEC.md` v1
- [x] Exact input access from Jprogram — resolved 2026-08-07 (LC reads both Jprogram's SQL DB and analysis files), **superseded 2026-08-09 by Owner's structural change**: Jprogram now stops at the parser output (canonical corpus JSONL + rename map). Language Coach owns everything downstream — analyzers, filter, reports, and its own SQLite library. See design spec §4 (revised) and the SQLite item below.
- [x] **Structural change + SQLite query library — decided and built 2026-08-09.** Owner restructured ownership: **Jprogram → parser output only; Language Coach owns analyzers/filter/reports/library.** Built: `tools/analysis/build_library_db.py` + `tools/analysis/library.db` (29 MB, 321 sources; schema: sources w/ full metadata [title/tier/difficulty/teacher/series joined from NJ catalog + rename map], frequency, exposure, chunks, known_words; all indexed). Reports = saved SQL queries delivered in chat (no report files — Owner decision); the one file artifact is the per-episode reader package (corpus JSONL + highlight list + grammar list) for QuadRead. Storage model: JSON outputs remain source of truth; SQLite is the query layer (Owner decision after scaling analysis: 1,806 sources ~1.1 GB JSON, queries 10-20s → SQL pays off). Metadata completeness caveat: NJ sources fully populated; NIJ/con-teppei nulls until their catalogs are joined (schema ready for it).
- [x] Whether Language Coach has any UI — resolved 2026-08-07: no standalone app; the chat session itself is the UI (design spec §5)
- [x] Platform/language choice — resolved 2026-08-07: Python, matching Jprogram (design spec §11)
- [x] Interim known-word bootstrap tool, where it lives — superseded 2026-08-07 by a concrete mechanism: consumption-history search → frequency list → test material → true level (design spec §8). The "where it lives" question is now folded into the tools/contract detailed-design item above, not a separate standalone tool.
- [x] Format/shape of Owner's consumption-history data — resolved 2026-08-07: Owner supplies it source-by-source directly in conversation (con-teppei ep 1-50 via existing OneDrive transcript+vocab-baseline files; nijapanese.com via its numeric `difficulty` field, ≤45; LingQ first 40 mini stories, manual). First real artifact built: `Shawn/teppei_1-50_self_assessment.tsv`, a frequency-sorted quick self-assessment list (simple format chosen over a context/quiz format, per Owner — can go deeper later).
- [x] Read-access workspace boundary — broadened 2026-08-07: effectively unrestricted (any location on disk, plus the open web), not just `C:\AI Development Projects\`. Write access unchanged: this workspace only, explicit permission required elsewhere. See `CLAUDE.md`.
- [x] Known-kanji baseline established (2026-08-07): first 400 kanji (by `Order`) in `C:\Japanese Learning\Anki projects\Kanji Speedrun 1200.txt` confirmed known by Owner. Extracted to `bootstrap/known_kanji_first400_speedrun.txt`.
- [x] Known-word granularity (lemma vs. surface form) — resolved 2026-08-07: **surface form**, tracked as distinct items. Owner's reasoning: not explicitly studying grammar/conjugation, success metric is content consumption, not abstract vocabulary size. See design spec §8a.
- [x] Reconcile LingQ's known-word data against the manual self-assessment — done 2026-08-07, see design spec §8b. Real correlation found (confidently-known words ~5x more likely to be LingQ-Known than the unconfident bucket), but neither dataset is complete alone. Working recommendation: LingQ as primary signal (level ≥4 tentative threshold), self-assessment as calibration/spot-check only.
- [x] Jprogram per-source metadata schema gap review — worked through with Owner 2026-08-07 while he was mid-task on Jprogram's actual schema. Five gaps identified and resolved (difficulty, teacher identity, title, traceability, season) — see design spec §12 for the full table. Net result: no nijapanese-specific fields added to Jprogram's schema (Language Coach cross-references the Content Collection catalog directly instead); real fixes were renaming `source_name`→`title` and adding a season field to the collection schema. `topicIds`/domain remains the one genuinely unimplemented gap, already on Owner's list to build.
- [x] Known-vocabulary source finalized — LingQ confirmed (not just "working recommendation") as primary source, level ≥4 = known (design spec §8b). **Update 2026-08-09 (QuadRead transition):** the reader stage is now **QuadRead** ("MiniLingQ", `C:\AI Development Projects\QuadRead`, V1 complete 2026-08-09, replaces retired QuadRead) with its own word ladder (unknown→new→learning→known→ignored) + separate grammar ladder in IndexedDB, and a backup-export mechanism (`minilingq-backup-YYYY-MM-DD.json`, `{app, version, exportedAt, dictionary[], defs[], grammar[]}` (STALE as of 2026-08-10, re-verify)). **However, QuadRead's dictionary has no real known-state data yet** — Owner confirmed 2026-08-09 the reader is waiting on Language Coach's *output* (highlight list) before its input side has meaning. **Therefore LingQ remains the LIVE known-word source for now** (working bootstrap, not just legacy), with the QuadRead word ladder as the future replacement once the feedback loop is populated. **Status mapping decided (2026-08-09):** when we switch, QuadRead `status == 'known'` counts as known — **confirmed to match LingQ levels 4-5** (i.e. the same known set semantics; level 4-5 = 822 words in the current LingQ export). learning/new/unknown don't count (`ignored` is a bonus signal — aligns with the loanword-filter "not a real gap" category). **Export landing location agreed (2026-08-09):** the QuadRead backup (`minilingq-backup-YYYY-MM-DD.json` (STALE as of 2026-08-10, re-verify)) lands in Language Coach's workspace — `bootstrap/reasonix_known_words.json` — same convention as the LingQ export. See `Shared/ECOSYSTEM_OVERVIEW.md` for the full handoff contracts.
- [x] Grammar known-state tracking — explicitly deferred to a future point, after real corpus data exists (design spec §8b addendum). **Verified state 2026-08-08: expression data is empty across the entire current corpus** (all 13 jsonl files — 8 NJ + teppei ep001/004/005 + tepei-test + qc-test — 0 expressions, 0 records with expressions). This is a *decided limitation with history*, not a bug: per Owner 2026-08-08, there is **no good deterministic expression-extraction library** for Japanese, and the cost-effective LLM alternatives proved **flakey** — so the parser emits no expression records. Consequences: `expression_analyzer` runs but always produces empty output; design-spec §"productive pattern vs. fixed idiom" value criteria can't operate on real data yet; `chunk` analysis (which does work — chunks are populated) is the current stand-in for grammar-pattern signal. Revisit only if a solid deterministic expression extractor emerges, or Owner opts back into LLM-based extraction.
- [x] **Grammar-pattern highlight list — deferred 2026-08-08 (Owner decision).** Asked "top 20 grammar patterns for reader highlighting"; investigation proved **chunks can't produce it**: GiNZA chunks are surface fragments (これは/これで/これも dominate), and of 33 probed real grammar constructions (てしまう, たことがある, ながら, でしょう, ていた, なければ...), only 9 appear as exact chunks at trivial counts — the rest split across chunk boundaries. Same root cause as empty expressions. Deterministic regex pattern-matching over corpus words would work (te-form+しまう, ながら, etc. — real occurrence counts), but **Owner chose to defer: reader highlights vocabulary only** for now. Revisit when/if grammar highlighting becomes a need.
- [x] **`comparison_analyzer` `by_item` verified working** (2026-08-08) — earlier probe falsely suggested it was empty; it contains 1,015 entries (per-item per-source occurrence map). No tool gap; the empty `by_item` report was a probe/display error, corrected.
- [x] User contract (§5) — resolved 2026-08-07: no upfront formal design. Document frequent routines after the fact instead, once they're real and repeated (same pattern already used for `LANGUAGE_COACH_LINGQ_EXPORT_PROCEDURE.md`). Revisit only if real friction shows up once tools exist.
- [x] **Analyzer tools built** (2026-08-07) — `tools/analysis/` now has isolated (not live-imported) copies of Jprogram's 7 analyzer modules, adapted to surface-form grouping, smoke-tested against synthetic data. Ready to point at real corpus data once Jprogram outputs it. See design spec §5/§6.
